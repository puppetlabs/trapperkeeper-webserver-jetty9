global: {
    # Points to a logback config file
    logging-config: examples/multiserver_app/logback.xml
}

nrepl: {
    enabled: true
}

webserver: {
    bar: {
        port:           8080
        default-server: true
    }
    foo: {
        port: 9000
    }
}

hello-web: {
    url-prefix = /hello
}

